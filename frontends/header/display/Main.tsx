import { Cemjsx, Func, Static, Fn } from "cemjs-all"
import logo from '@images/logo/logo.svg'


export default function () {
  return (
    <header class="header">
      <div class="container mx-auto py-2">
        <img src={ logo } alt="logo, crypto emergency" />
      </div>
    </header>
  )
}